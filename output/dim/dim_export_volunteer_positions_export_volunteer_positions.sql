
CREATE TABLE dim.export_volunteer_positions_export_volunteer_positions (
    first_name VARCHAR(255),
    last_name VARCHAR(255),
    company VARCHAR(255),
    volunteer_position VARCHAR(255),
    last_contact VARCHAR(255),
    do_it_again? VARCHAR(255),
    volunteer_note VARCHAR(255)
);